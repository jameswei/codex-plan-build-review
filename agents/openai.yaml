interface:
  display_name: "Plan, Build, Review"
  short_description: "Plan, review, and build scoped work task by task"
  default_prompt: "Use $plan-build-review to plan and independently review this feature or milestone, then stop for my approval before building, reviewing, and checkpointing each task."
policy:
  allow_implicit_invocation: false
